# ER Module — Knowledge

ความรู้ที่ต้องใช้ก่อนแก้โมดูล ER ของ HOSxP Plus V5 (redesign บนแท็บเล็ตแนวนอน) รวมกติกาที่ตกลงกับผู้ออกแบบไว้แล้ว
อ่านคู่กับ `docs/er-overview-requirement.md` (หน้าภาพรวมห้อง)

- สถานะข้อมูล: **จำลองทั้งหมด** ยังไม่ต่อฐานข้อมูล HOSxP จริง
- branch: `feature/er-bed-view-3d`
- รันตอนพัฒนา: `flutter run --dart-define=START=/erFlowHome` (ค่าเริ่มต้นใน `nav.dart` คือ `'/'` ไม่ต้องแก้ไฟล์ก่อน commit)

---

## 0. ความรู้เชิง business / การแพทย์ที่ได้จากผู้ใช้ (domain rules)

เรื่องที่ไม่ได้อยู่ในโค้ดหรือเอกสารเดิม ได้มาระหว่างออกแบบร่วมกับผู้ใช้ ใช้เป็นกติกาตัดสินใจ

| # | กติกา | ผลต่อการออกแบบ |
|---|---|---|
| 1 | แต่ละบทบาทรู้ข้อมูลต่างกัน: ข้อมูลเหตุการณ์ (หมวกนิรภัย เข็มขัด แอลกอฮอล์ ยานพาหนะ การดูแลก่อนมาถึง) ได้จากผู้ป่วย/ญาติ/ผู้นำส่ง ตอน **คัดกรอง** แพทย์ไม่มีทางรู้ | ฟอร์มแยกตามบทบาท แพทย์/พยาบาล ER/พยาบาลคัดกรอง ห้ามถามข้ามบทบาท |
| 2 | ข้อมูลคัดกรองพยาบาลบันทึกแล้ว แพทย์แค่ **ทบทวนและยืนยัน** ไม่กรอกซ้ำ | ขั้นแรกของแพทย์ = ยืนยันข้อมูลคัดกรอง + แพ้ยา |
| 3 | แพทย์ที่มาดูผู้ป่วย ER บางคนเป็น **แพทย์เฉพาะสาขา** (กระดูก สูติ ศัลย์ ฯลฯ) ตรวจเฉพาะระบบของตน ระบบอื่นไม่ได้ตรวจ | template ตรวจร่างกาย = ขอบเขตระบบของสาขา · ระบบนอกขอบเขต = "ไม่ได้ตรวจ" |
| 4 | ผลตรวจ **ผิดปกติต้องมีคำอธิบายเสมอ** (เวชระเบียนต้องบอกว่าผิดปกติอย่างไร) | บังคับรายละเอียด ยืนยันไม่ได้จนกว่าจะใส่ |
| 5 | HPI ในฟอร์มจริงเป็น **ข้อความช่องเดียว** แพทย์เขียนเป็นย่อหน้าตามแบบแผน (OLDCARTS / กลไกการบาดเจ็บ) | ไม่แตกเป็นหลายช่อง ใช้ template ย่อหน้าที่มี [ ] ให้เติม |
| 6 | รหัสหัตถการ ICD-9-CM ผูกกับหัตถการแน่นอน (เช่น เย็บแผล → 86.59) | แนะนำจาก master ไม่ให้ AI เดารหัส |
| 7 | ตำแหน่งบาดแผลระบุด้วยส่วนของร่างกาย + ข้าง (ซ้าย/ขวาของผู้ป่วย) | แตะบนหุ่น 3D แล้วแปลงเป็นชื่อส่วน+ข้าง |
| 8 | ห้อง ER มีเสียงรอบข้างตลอด (คนคุยกัน อุปกรณ์) ไมค์เปิดค้างจะรับเสียงที่ไม่เกี่ยวกับเคส · ผู้ใช้ต้องรู้ว่า AI รับคำสั่งไปตีความครบเมื่อไหร่ | แตะเปิด/ปิดไมค์เอง (ห้ามเปิดไมค์เอง) · ระหว่างเปิด **ตีความแบบ real time**: หยุดพูด ~0.8 วิ = ตัดประโยคส่งถอดเสียง+ตีความทันที ไมค์ฟังต่อ · แถบสถานะเหนือไมค์ ฟัง → ถอดเสียง → ตีความ (+ ได้ยินว่า… / ลงแล้ว: ช่อง / รออีก n ประโยค) + **live caption** (ถอดเสียงประโยคที่กำลังพูดทุก 1.2 วิ เป็นข้อความชั่วคราวตัวเข้ม · ประโยคที่จบแล้วเป็นตัวเทา) · ไมค์เปิดอยู่ผู้ช่วยไม่พูดออกเสียง · ต้องมีหน้าสรุปยืนยันก่อนบันทึก |
| 9 | แพทย์ต้องการเห็น **แนวโน้มระหว่างการตรวจแต่ละครั้ง** (แย่ลง/ดีขึ้น/พบใหม่) มากกว่าผลครั้งเดียว | แท็บตรวจร่างกายเป็นไทม์ไลน์เทียบครั้งก่อน |
| 10 | ข้อมูลที่หน้าจอแสดงอยู่แล้ว ผู้ช่วยไม่ต้องพูด/แสดงซ้ำ แพทย์ต้องการคำตอบสั้นแบบวิทยุ | ผู้ช่วยตอบ ≤ 2 ประโยค และไม่สร้างการ์ดข้อมูลซ้ำ |
| 11 | อาการที่ไม่ระบุอวัยวะชัด (ปวดท้อง คลื่นไส้) ไม่ควรชี้อวัยวะใดอวัยวะหนึ่ง | แสดงเป็น heatmap โซน ไม่ map อวัยวะ |
| 12 | ทุก workflow ต้อง **ยืนยันก่อนบันทึก** เห็นสรุปทุกช่องอีกครั้ง | หน้าสรุปก่อนบันทึกท้ายทุกขั้น |
| 13 | ผู้ป่วย **สังเกตอาการ (observe)** ใน ER เป็นช่วงงานแยกจากหลังการตรวจ ต้องติดตามว่าอยู่นานเท่าไร (เกณฑ์ไม่เกิน 6 ชม. ก่อนตัดสิน admit/จำหน่าย — ค่าตั้งต้น รอยืนยันกับหน้างาน) | เมนู "สังเกตอาการ" ในแถบซ้ายใต้หลังการตรวจ · ความจุ/เวลาค้างแยกของตัวเอง |
| 14 | การ์ดผู้ป่วยต้องแสดง **QN (เลขคิว) คู่กับเลขเตียง** เพราะหน้างานเรียกผู้ป่วยด้วยคิวก่อนได้เตียง | มุมขวาของการ์ดใน footer: QN + เตียง (QN ยังเป็นค่าจำลอง) |
| 15 | HPI เป็นข้อความช่องเดียวก็จริง แต่ต้อง **อ่านง่ายแบบจัดบรรทัด** ขึ้นบรรทัดตามหัวข้อทางคลินิก: อาการหลัก → เวลาเริ่ม/Last seen → ลักษณะ/ร้าวไป → อาการร่วม → ความรุนแรง → ประวัติ → ยา → การดูแลก่อนมา · ต้อง **จัดอัตโนมัติแบบ prettier** ไม่ใช่ให้ผู้ใช้กดจัดเอง | ทุกค่าที่เข้า HPI (พิมพ์ · พูด · template · AI) จัดบรรทัดทันที · แถบเหนือช่องบอก "จัดรูปแบบอัตโนมัติ" + ปุ่มแก้ไข |
| 16 | พยาบาลคัดกรองต้องได้ **สรุปความเร่งด่วนพร้อมระดับ ESI ที่แนะนำ** จากการทบทวนเคส (ESI v4: ช่วยชีวิตทันที → เสี่ยงสูง/สับสน/ปวดรุนแรง → จำนวนทรัพยากร → สัญญาณชีพโซนอันตราย) พยาบาลเป็นผู้ยืนยันเสมอ | การ์ด "สรุปความเร่งด่วนโดย AI" ในขั้นระดับ ESI ของพยาบาลคัดกรอง: ระดับ + เหตุผล + ปุ่มใช้ระดับนี้ · เตือนเมื่อต่างจาก ESI ที่บันทึกไว้ |
| 17 | สั่ง **Admit ต้องเลือกตึกผู้ป่วยในปลายทาง** (Refer ต้องเลือกสถานพยาบาล) และตึกต้องเข้ากับเคส: เพศ (ตึกชาย/หญิง) อายุ (เด็ก → กุมาร) สาขาตามวินิจฉัย (กระดูกหัก → ออร์โธ, head injury → ศัลยกรรมประสาท, stroke → Stroke Unit, STEMI → CCU) และความรุนแรง (ESI 1 → ICU) | ช่องปลายทางในขั้นจำหน่ายเปลี่ยนตามสภาพผู้ป่วยออกจาก ER: Admit = รายการตึก (master er_ipd_ward) เรียงตามความเหมาะ + ชิปแนะนำ 3 อันดับ · Refer = สถานพยาบาล (er_refer_hospital) · เปลี่ยนสภาพแล้วล้างปลายทางเดิม |
| 18 | ช่วง **สังเกตอาการ** สิ่งที่ต้องเห็นเกี่ยวกับผู้ป่วยคือ **กิจกรรมพยาบาลที่บันทึกไปแล้ว** ส่วนใหญ่เป็นข้อความอิสระพร้อมเวลา (ประเมินซ้ำ การพยาบาลที่ให้ ผลตอบสนอง) | การ์ดผู้ป่วยในหน้าสังเกตอาการแสดงไทม์ไลน์ "กิจกรรมพยาบาล" ใหม่ → เก่า: เวลา · ข้อความ · ผู้บันทึก (ข้อมูล `ErCase.nurseNotes`) แทนสรุปโดยระบบ |
| 19 | คำพูดที่บันทึกด้วยเสียง **ต้องย้อนกลับไปแก้ได้** และผู้ช่วยต้อง **ตีความจากทุกประโยครวมกัน** ไม่ใช่แค่ประโยคล่าสุด (ตามแบบ CIS 2026 OPD) · ใช้กับ HPI เป็นหลัก | เก็บคำพูดเป็นรายประโยคต่อขั้น (ไม่แสดงแยก) ตีความลง **template HPI ที่แพทย์เลือก** เติมแทน "[คำใบ้]" ที่ยังไม่พูดคงไว้ · ยังไม่เลือก = ใช้ template ที่ระบบแนะนำ · ประโยคที่แก้ประโยคก่อน ("ไม่ใช่… เป็น…") ไปแก้ค่าในช่องเดิม · HPI แก้แบบผ่าตัด คงถ้อยคำเดิม แก้เฉพาะวลีที่เปลี่ยน ข้อมูลที่ถูกถอนคืนเป็น [ ] · ช่องที่เปลี่ยนไฮไลต์ชั่วครู่ |
| 20 | รายการใน Order Set ที่เลือกแล้วต้อง **ระบุรายละเอียดตามฟอร์ม HOSxP** ก่อนสั่ง: **ยา** = ติ๊ก **ยา STAT** (ให้ทันทีครั้งเดียว · ป้าย STAT ข้างชื่อยา) · วิธีใช้ Mode 2 (เลือกจากฉลากช่วย/พิมพ์เอง) หรือ Mode 3 (Dose · หน่วย · ความถี่ · เวลา) + จำนวน + หน่วยบรรจุ · **เลือด** = วันเวลาที่ใช้ · ความต้องการ · ความเร่งด่วน · แพทย์ผู้ขอ (= account ที่ login แก้ไม่ได้) · จุดที่ขอ · หมายเหตุ · จำนวน cc · ราคา · **Set OR** = วันเวลาเข้าห้องผ่าตัด · ถึงวันเวลา · ห้องผ่าตัด | ติ๊กรายการแล้ว **แผงขวาเปลี่ยนเป็นฟอร์มกรอกของรายการนั้นทันที** ไม่มีหน้าซ้อน/แผ่นลอย ค่าบันทึกขณะกรอก ตัวเลือกสั้นเป็นชิป ยาวเป็นเมนูลง วันเวลาเป็นชิปวัน + ช่องเวลา (`features/patient/order_detail.dart`) · ตัวเลือกจาก master (`er_drugusage` `er_drug_unit` `er_drug_frequency` `er_drug_time` `er_drug_package_unit` `er_order_priority` `er_room` `er_or_room`) · ความต้องการเลือดและราคายังเป็นค่าจำลอง |
| 21 | Order Set มี **template ตามประเภทผู้ป่วย** (เช่น งูกัด → Standing order Snake bite รพ.ปากเกร็ด ต.ค.65) · template แบบ standing order เขียนเป็น **PROGRESS NOTE (ประเมิน) คู่กับคำสั่ง** ที่เปลี่ยนตามผลประเมิน: ชนิดงู → สาขา hematotoxin (1.x รวมไม่ทราบชนิด) / neurotoxin (2.) · เกณฑ์ antivenom → 1.1 ให้ antivenom (กะปะ/เขียวหางไหม้ 3 vials · แมวเซา 5 · รวม hemato 5 · ซ้ำได้ทุก 6 hr) หรือ 1.2 ไม่ให้ · neurotoxin: เห่า/จงอาง/สามเหลี่ยม 10 vials · ทับสมิงคลา 5 · ซ้ำได้ทุก 2-6 hr · ทับสมิงคลา/สามเหลี่ยมให้โดยไม่ต้องรออาการ | หน้าเลือก Order Set = **การ์ด 2 คอลัมน์ (รูป · ชื่อ · ที่มา) ทุกชุด** ไม่มีช่องค้นหา/ชิปประเภทผู้ป่วย · ชุดติ๊กรายการ = เลือกแล้วรายการขึ้นด้านล่าง · เลือก template แล้วเข้าอีกชั้น (drill-in): rail workflow ยังอยู่ หัวข้อ progress note (Fang mark · ชนิดงู · Antivenom · Intubation · Refer · สรุป) เป็น **sub menu ใต้ขั้นวินิจฉัย/สั่ง** · ปุ่ม × ปิด template · แตะขั้นอื่น = ออกจาก template · แผงกลาง = ประเมินหัวข้อนั้น · แผงขวา = คำสั่งตามลำดับเอกสาร (ONE DAY → CONTINUATION → MED → หมายเหตุ) เฉพาะที่เกี่ยวกับหัวข้อที่เลือก · สรุป = ทั้งหมด + บันทึก · **แถบสาขาคำสั่ง** (1.1/1.2/2.) พร้อมชิปเหตุผล (ชนิดงู + เกณฑ์ที่ติ๊ก) · **ผล lab ช่วยประเมิน** Plt<50k/INR>1.2, WBCT>20, AKI (Cr สูง · ใช้เฉพาะงูแมวเซา) แสดงค่า + แนะนำ ให้แพทย์ยืนยัน ไม่ติ๊กเอง · **หมายเหตุ antivenom ผูกใต้คำสั่ง AV** (ไม่ต้อง skin test · เฝ้า 2 ชม.) + ปุ่ม anaphylaxis → หยุด AV + Adrenaline 0.5 ml IM + CPM 10 mg IV ขึ้นบนสุด · **คอลัมน์ Date = รอบประเมิน**: บันทึกแล้วนับรอบ ประเมินซ้ำ +6 ชม. (neuro ที่ให้ AV +2 ชม.) · Intubation/Compartment → แนะนำ Refer · ช่อง ml/hr ว่าง = บันทึกไม่ได้ · หน้าสรุปมีมุมมอง **กระดาษ 3 คอลัมน์** เหมือนเอกสารเดิมไว้ตรวจทาน (`features/order_template/`) |
| 22 | **อาการสำคัญ (CC) บันทึกได้หลายคน** (พยาบาลคัดกรอง แพทย์ พยาบาลประจำเตียง) ข้อความแต่ละคนอาจต่างกัน ต้องเลือกดูของแต่ละคนได้ · HPI ต้องเห็นในหน้าภาพรวมคู่กับ CC | การ์ด CC ในแผงขวา: ชิปผู้บันทึก·เวลา (ค่าเริ่มต้น = ล่าสุด) + "บันทึกโดย…" + HPI ใต้เส้นคั่น (HPI จาก workflow ถ้าเขียนเสร็จ ไม่งั้นของในแฟ้ม) · ข้อมูลจาก `erCcRecords()` รายการแรก = คัดกรองจริง ที่เหลือจำลอง · ชิป **สรุป AI** รวมทุกผู้บันทึก ทุกช่วงข้อความมีเลขอ้างอิง ①②③ = ใคร·บทบาท·เวลา + **อาการตามช่วงเวลา** (ไทม์ไลน์: เวลาที่บันทึก · ห่างจากครั้งก่อน · ผู้บันทึก · อาการที่เปลี่ยน/พบใหม่) + ข้อแตกต่างระหว่างผู้บันทึก (`features/patient/cc_summary.dart`) |
| 23 | header ผู้ป่วยต้องมี **pain score** (ต่อจาก ESI) และ **รูม่านตา** | pain ใช้ค่าและรูปหน้าจาก item_pain_score ของ HOSxP+ แต่หน้าตาเป็น pill ขาวนูน แดงเฉพาะ ≥ 7 · รูม่านตาจาก `erPupilOf()` (ช้า = แดง) · คัดลอกแบบแท็บเล็ต: **กดค้างที่ก้อนข้อมูล = คัดลอกทั้งก้อน** (CC · HPI · ค่าใน header · Lab) + ปุ่มคัดลอกที่หัวการ์ด (CC+HPI · V/S · Lab · visit ใน EMR) · แจ้งผลพร้อมปุ่ม "ใส่ progress note" · ไม่ใช้การลากเลือกข้อความ (`copy_tools.dart`) |
| 24 | ตำแหน่งอาการที่ plot บนหุ่น ต้อง **กดเข้าไปดูรูปได้** (drill-down) | แตะป้ายอาการ → แผงทับหุ่น: วินิจฉัยที่ตรงตำแหน่ง (ICD-10 · วิกฤต) · ภาพ X-ray/CT ของเคสที่ตรงบริเวณ · รูปกายวิภาค BodyParts3D (`assets/images/bodymap/`) · ปุ่ม ← กลับ |
| 25 | ผล Lab ต้อง **เลือกดูตามวันที่มา visit ได้** รวมของวันนี้ | การ์ดผลแล็บ: ชิป "วันนี้" + วันที่ visit ก่อนหน้า (ใหม่ → เก่า) · visit เก่าแสดงสถานที่·วันที่ใต้หัวการ์ด · ข้อมูล `erLabVisits()` visit ก่อนหน้าเป็นค่าจำลอง (มีเฉพาะเคส A1) |
| 26 | **อายุต้องแสดงเป็น "ปี เดือน วัน" เสมอ** (เช่น 54 ปี 0 เดือน 16 วัน) | ใช้ `ErCase.ageText` ทุกจุดที่แสดงอายุ · ตอนนี้เดือน/วันคำนวณจาก HN (จำลอง รอวันเกิดจริง) · ตรรกะที่ใช้อายุ (เด็ก/ผู้ใหญ่) ยังใช้ `age` เป็นปี |
| 27 | ผู้ป่วยมี **โรคประจำตัว / แพ้ยา หลายรายการ** ได้ | ใน header แสดงจำนวนในวงเล็บ และเลื่อนแนวนอนดูได้ครบ (ขอบขวาจาง = ยังมีต่อ) ไม่ตัดด้วย … |
| 28 | ผล Lab มี **3 ชนิด: numeric · text · image** และต้อง **เลือกหลายรายการแล้วเปรียบเทียบการเปลี่ยนแปลงข้าม visit** ในแท็บแล็บ (ไม่ใช่หน้าภาพรวม) | `ErLab` / `ErLab.text` / `ErLab.image` · แท็บแล็บ: การ์ดเปรียบเทียบ (กริดแตะเลือก → ตาราง visit เก่า→วันนี้ + ↑↓ ผลต่าง %) แล้วตามด้วยตารางผลเต็ม · ภาพรวม: แตะการ์ด Lab = ไปแท็บแล็บพร้อมเลือกรายการนั้น · รูป lab ตัวอย่างเป็น mock (`assets/images/lab/`) |
| 29 | สัญญาณชีพต้องมี **Pulse** · สัดส่วนร่างกาย **น้ำหนัก (kg) ส่วนสูง (cm) BMI (kg/m²) BSA (m²)** ต้องมีหน่วยเสมอ | การ์ดสัญญาณชีพเป็นการ์ดแรกของภาพรวม · แตะ/ลากแท่งดูแต่ละรอบ + เวลาวัดใต้แท่ง · แถวสัดส่วนร่างกายใต้การ์ด (BSA สูตร Mosteller) · Pulse ตอนนี้ใช้ค่าชุด HR (จำลอง) |
| 30 | สิ่งที่แพทย์ดูข้างเตียง: **Lab · V/S · X-ray · EMR** · ใน Lab ต้องเห็น **eGFR** (profile ที่ดูประจำ) · เขียน **progress note** โดย copy ประวัติ/CC/HPI ลงไป | การ์ดแล็บ: toggle "eGFR" = กรองการ์ดเหลือเฉพาะ lab ใน profile (eGFR คำนวณ CKD-EPI 2021 จาก Cr · BUN · Cr · Na · K · Cl · HCO₃) ปกติ ≥ 60 · แท็บ **EMR** (ทุก visit: CC · HPI · Lab ผิดปกติ · ปุ่มใส่ progress note) · แท็บ **Progress note** (ช่องเขียน + ปุ่มแทรก CC/HPI/ประวัติ/V/S/Lab ผิดปกติ/eGFR/Dx ที่ cursor + คัดลอกทั้งหมด) · การ์ด CC มีปุ่ม "ใส่ CC + HPI ใน progress note" |
| 31 | HPI เริ่มแบบ **เล่าเอง** ได้ (ไม่บังคับ template) แล้วค่อยเลือก template มาจัดทีหลัง | ปุ่ม "เพิ่ม HPI ด้วยตนเอง" → ฟอร์มเปล่า + speech to text · แถบใต้ช่อง "จัดลง template" ให้ AI เติม [ ] จากที่เล่า (ไม่ทิ้งข้อมูล ไม่เดา) · การ์ด CC มีชิปเลือกดู HPI ของ visit ก่อนหน้า |
| 32 | ขั้น **"ออกจาก ER" ของพยาบาล** ให้เห็นทุกช่องพร้อมกัน (ไม่ต้องไล่ทีละหน้า) · สภาพผู้ป่วยออกจาก ER เลือกจากการ์ดในหน้า · วันที่-เวลาออกใช้ date time picker + ปุ่มเวลาปัจจุบัน | `_formAllAtOnce` = ฟอร์มหน้าเดียวเลื่อนดูได้ (stepper เหลือหน้า "แบบฟอร์ม" หน้าเดียว) · `_dispCards` 3 คอลัมน์ · `_dateTimeField` (พ.ศ. dd/MM/yyyy HH:mm น.) ใช้กับทุกช่องที่ชื่อมี "วันที่" + "เวลา" |
| 33 | แท็บภาพรวมแผงขวาเป็น **bento** ขนาดช่องตามความสำคัญข้างเตียง | V/S เต็มแถว → CC/HPI (3/5) + X-ray · งานที่ต้องติดตาม · วินิจฉัย (2/5) → Lab เต็มแถว → แผนการดูแล (3/5) + ทางลัด EMR · Progress note (2/5) · ช่องเล็กแตะไปแท็บเต็ม · แผงแคบ < 560 = คอลัมน์เดียว · แผงขวาเริ่มที่ 60% ของจอ ลากที่จับขอบซ้ายปรับได้ 45–70% · แตะสองครั้ง = ค่าเริ่ม (`features/patient/overview_bento.dart`) |
| 34 | widget หน้าภาพรวม **เรียงข้อมูลอัตโนมัติเสมอ** (สิ่งที่ต้องสนใจขึ้นก่อน) | งาน: ยังไม่ทำ → ด่วน → สั่งก่อน · ทำแล้วลงล่างสุด · Lab: ผิดปกติ → ตัวเลขปกติ → ข้อความ → รูป (eGFR หัวเสมอ) · วินิจฉัย: critical → urgent → normal · ในกลุ่มเดียวกันคงลำดับเดิม |
| 35 | **จุดแดง = มีของใหม่ที่ยังไม่เปิดดู** (ผลแล็บออกใหม่ / คำสั่งแพทย์ใหม่) | จุดแดงมุมขวาบน: ปุ่ม คำสั่งแพทย์ · ผลแล็บ บนการ์ดรายเตียง · แท็บ คำสั่งแพทย์ / แล็บ · การ์ดแล็บรายการที่ออกใหม่ · หายเมื่อเปิดแท็บนั้น (ต่อเคส) · ข้อมูลใหม่ตอนนี้เป็น mock (`erLabNew` / `erOrderNew`) |
| 36 | **วัดสัญญาณชีพซ้ำ** จากการ์ดสัญญาณชีพ | ปุ่ม "วัดซ้ำ" → ลิ้นชักขวา กรอก HR · BP บน/ล่าง · SpO₂ · RR · BT (เว้นว่าง = ใช้ค่าเดิม · นอกเกณฑ์ขึ้นแดง) · พูดกรอกแบบ real-time เหมือน workflow (ฟัง → ถอดเสียง → ตีความ ทุกครั้งที่หยุดพูด + คำบรรยายสด) · บันทึกแล้วต่อท้ายกราฟ · **แพทย์สั่งวัดซ้ำเป็นรอบ** ครั้งเดียว / ทุก 15·30 นาที·1·2·4 ชม. × 2–8 ครั้ง → งานพยาบาลตามเวลา · SpO₂ ไม่มีสูตรคำนวณจาก V/S อื่น (ต้องวัดจากเครื่อง) · กราฟ BP แสดงสองเส้น ตัวบน/ตัวล่าง |
| 37 | Progress note: **AI ประเมินเคส** แล้วแพทย์เลือกหัวข้อ | ปุ่ม "ให้ AI ประเมินหัวข้อ" → AI อ่านเคส (CC/HPI · V/S แนวโน้ม · Lab · ภาพ · ยา · Dx · งานค้าง) เสนอ 5–8 หัวข้อแบบ SOAP พร้อมข้อความ · ค่าเริ่มติ๊กทุกข้อ แพทย์ติ๊กออกได้ · "บันทึกลง progress note (n)" จัดกลุ่ม S/O/A/P แทรกที่ตำแหน่งเคอร์เซอร์ · ใช้เฉพาะข้อมูลในเคส ห้ามแต่งค่า · ประเมินใหม่ได้ |
| 38 | ป้ายบนหุ่น 3D บอก**ที่มาของจุด** | ป้ายขาวหน้าชื่อ: **แผล** = ตำแหน่งแผล (ช่องแผลใน workflow หรือคำว่า แผล/ฉีกขาด/ถลอก/laceration ติดตำแหน่ง) · **DX** = การวินิจฉัย (รวมผลภาพถ่าย) · **PE** = ตรวจร่างกายรอบล่าสุด เฉพาะที่ผิดปกติ · **CC** = อาการสำคัญ/HPI · ลำดับตัดสิน แผล → DX → PE → CC |
| 39 | หุ่น 3D **ตามเพศผู้ป่วยจริง** · มุมมองแบบหันหน้าเข้าหาผู้ดู | แอปส่ง `female` ต่อเตียง (`ErRoomBed.female`) · ในไฟล์ `er_patient_figure.glb`: Armature_60 = หญิง · Armature.001_121 = ชาย (วัดสัดส่วนสะโพก/อก) · ผู้ป่วยไม่มีเตียงใช้เพศของตัวเองบนเตียงที่ยืมแสดง · มุมมองมาตรฐาน anatomical position: ซ้ายของผู้ป่วยอยู่ขวาของจอ |
| 40 | **ลงทะเบียนผู้ป่วยใหม่** เก็บเฉพาะข้อมูลประจำตัว | ปุ่มบนสุดของหน้าแรก (ภาพรวมห้องฉุกเฉิน) → หน้าเต็มจอ (แทน `regis_patientipd_e_r` เดิม) · ข้อมูลการมา V/S GCS ESI ไม่ถามซ้ำ อยู่ใน workflow คัดกรอง · บังคับแค่ชื่อ + เพศ · "ไม่ทราบชื่อ" = ผู้ป่วยนิรนาม ลงทะเบียนก่อนแก้ชื่อทีหลัง · แพ้ยา: ชิปแดง / ติ๊ก "ไม่มีประวัติแพ้" (ต่างจากยังไม่ได้ถาม) · อ่านบัตรประชาชนเติมข้อมูล (ตอนนี้จำลอง) · บันทึกแล้วเข้าคิวรอคัดกรองทันที |
| 41 | **สแกนหน้าค้นหาผู้ป่วย** ต้องยืนยันก่อนเปิดเสมอ | ปุ่ม "สแกนหน้า" คู่ปุ่มลงทะเบียน → กล้องหน้า → แสดงผู้ป่วยที่คล้ายสุด 3 ราย (รูป ชื่อ HN อายุ เตียง % ความคล้าย) ให้กด "เปิด" เอง ห้ามเปิดเคสอัตโนมัติ (กันเปิดผิดคน) · ไม่พบ → "ลงทะเบียนใหม่" · การ์ดผู้ป่วยที่ดูล่าสุด: map ตำแหน่งบนหุ่นไม่ได้ (เช่น เบาหวาน ความดัน) = ไม่แสดงหุ่น |
| 42 | **ทุก flow: กรอกครบ หรือสั่งข้าม → ไปหน้าตรวจสอบ** (ไม่ข้ามขั้นเงียบ ๆ) | ตีความแล้วช่องบังคับครบ = พาไปหน้าตรวจสอบเอง · สั่ง "ข้าม"/"ขั้นต่อไป" = ไปหน้าตรวจสอบของขั้นนี้ก่อน ปุ่มเป็น "ข้ามขั้นนี้ · ขั้นต่อไป" แม้ยังไม่ครบ · อยู่หน้าตรวจสอบแล้วสั่งอีกครั้ง = ยืนยันไปขั้นต่อไป · "ยังไม่ได้ตรวจ/ไม่ได้ทำ/ไม่มี ทั้งหมด" = ใส่ค่านั้นทุกช่องที่ว่าง · ตรวจร่างกาย: ช่อง "บันทึกการตรวจแบบละเอียด" ไม่บังคับ |
| 43 | **F9 = ส่งต่อผู้ป่วยไป flow ถัดไป** (คีย์ยืนยันของ HOSxP) | ปุ่มมุมขวาบนหน้าผู้ป่วย "ส่งต่อ · <ขั้นถัดไป> F9" / คีย์ F9 → ลิ้นชักขวาแบบหน้าจอการส่งต่อของ HOSxP: **ส่งต่อห้อง** (ตั้งต้นตามการจำหน่าย: Admit/Refer/กลับบ้าน ไม่มี = การเงิน · ขั้นอื่น = ห้องตรวจฉุกเฉิน) · **สถานะ** (ตั้งต้น = ขั้นถัดไป: รอคัดกรอง → รอตรวจ → ตรวจแล้ว → รอออก → ออกจาก ER · สังเกตอาการ → รอออก) · **จำนวนเงินรอทำรายการชำระ** (บาท แดงเมื่อ > 0) · **จำนวนรายการยา** · ปุ่ม/คีย์ "ยืนยัน (F9)" · คัดกรองยังไม่มี ESI = เตือนแดง · แจ้งเตือน/ประวัติการบันทึกอยู่มุมซ้ายล่าง |

---

### สิ่งที่แพทย์สนใจตอนดูแลเคสหน้างาน (ข้างเตียง) — จากผู้ใช้

- Lab · V/S · X-ray
- EMR (tab)
- ตรง Lab: **eGFR** เป็น profile ที่ดูประจำ ต้องมี toggle การแสดงผลส่วนนี้ใน panel Lab
- การเขียน progress note: copy-paste ประวัติ, CC, HPI เอาไปใส่ใน progress note

(ออกแบบแล้วตามกติกาข้อ 30)

---

## 1. แผนที่ไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `lib/er/er_flow_home/er_flow_home_widget.dart` | shell: imports, `part` ทั้งหมด, state การนำทาง, initState/dispose/build |
| `lib/er/er_flow_home/{core,sidebar,tabs,features}/` | โค้ดแต่ละ tab/feature แยกไฟล์ให้หลายคนทำพร้อมกันได้ (ดูตารางถัดไป) |
| `lib/er/er_flow_home/er_detail_tables.dart` | ตารางข้อมูลของแต่ละแท็บ (มุมมองตาราง) |
| `lib/er/er_login/er_login_widget.dart` | login จำลอง เลือกบทบาท (ไม่ใช้ Provider ID / PIN จริง) |
| `lib/er/er_shared/er_session.dart` | ผู้ใช้ปัจจุบันและบทบาท `ErRole { doctor, nurse, triage }` |
| `lib/er/er_shared/er_cases.dart` | เคสจำลอง 20 ราย (อาการสำคัญแบบ ER จริง) |
| `lib/er/er_shared/er_case_tables.dart` | ข้อมูลตารางต่อเคส ต่อแท็บ |
| `lib/er/er_shared/er_ai.dart` | เรียก LLM / ASR / TTS ของ BMS Cloud |
| `lib/er/er_shared/er_genui.dart` | แค็ตตาล็อก generative UI (ชนิดบล็อก + prompt) |
| `lib/er/er_shared/er_form_kb.dart` + `assets/jsons/er_form_kb.json` | ฐานความรู้ฟอร์ม HOSxP (ช่อง ชนิด ตัวเลือก บทบาท) |
| `lib/er/er_shared/er_master.dart` + `assets/jsons/er_master_data.json` | master data ของ dropdown ทุกช่อง (66 ตาราง) |
| `lib/er/er_shared/er_pe_templates.dart` | template การตรวจร่างกายของแพทย์ (เก็บในเครื่อง) |
| `lib/er/er_shared/er_body_map.dart` | map อาการสำคัญ → อวัยวะ / กระดูก / โซน heatmap |
| `lib/er/er_bed_view/er_room_3d_io.dart` | ฉาก 3D (Three.js r128 ใน WebView): ห้อง เตียง หุ่น ชั้นกายวิภาค |
| `scratchpad/bp3d/*.py` | สคริปต์สร้างโมเดลจาก BodyParts3D (กระดูก อวัยวะ ภาพการ์ด) |

### โครงไฟล์ `er_flow_home/` (แยกตาม sidebar tab และ feature)

ทุกไฟล์เป็น `part of` library `er_flow_home_widget.dart` ใช้ของ private (`_blue`, `_t()` …) ร่วมกันได้

- **state ของ feature** อยู่ใน `mixin _XxxState on State<ErFlowHomeWidget>` ในไฟล์ของ feature นั้น (State หลัก `with` ทุก mixin)
- **logic + UI ของ feature** อยู่ใน `extension _XxxPart on _ErFlowHomeWidgetState` ไฟล์เดียวกัน
- **ไฟล์หลัก** (`er_flow_home_widget.dart`) เหลือแค่ shell: imports, `part` list, state การนำทาง (`_open`, `_detail`, `_detailTab` …), `initState`/`dispose`/`build`
- เพิ่ม feature ใหม่: สร้างไฟล์ในโฟลเดอร์ที่ตรง + `part` ในไฟล์หลัก · field ใหม่ใส่ใน mixin ของ feature · ค่าคงที่ใส่เป็น top-level private

| โฟลเดอร์ | ไฟล์ | หน้าที่ |
|---|---|---|
| `core/` | `theme` · `models` · `mock_data` · `widgets` · `skeleton` | token/gloss (แก้ต้องผ่าน design owner), enum+โมเดล, ข้อมูลจำลอง, widget ใช้ร่วม, skeleton |
| `sidebar/` | `sidebar` · `pinned` | แถบไอคอนซ้าย + ป้ายผู้ใช้ · ผู้ป่วยที่ปักหมุด |
| `tabs/` | `left_panel` | ตัวสลับแผงซ้ายตาม tab ที่เลือก |
| `tabs/overview/` | `overview_tab` · `overview_scene` · `urgent_strip` | **tab ภาพรวม**: แผงสรุป, ฉาก isometric + ซูม, แถบผู้ป่วยเร่งด่วน |
| `tabs/phase/` | `phase_tab` · `patient_list` · `recent` · `observe` | **tab ช่วงงาน** (คัดกรอง/ตรวจรักษา/หลังตรวจ/สังเกตอาการ ใช้แผงเดียวกัน): รายชื่อ, ผู้ป่วยล่าสุด, กิจกรรมพยาบาลของสังเกตอาการ |
| `features/patient/` | `patient_page` · `patient_header` · `overview_panel` · `overview_bento` · `cc_summary` · `body_scene` · `exam_tab` · `orders_tab` · `order_detail` · `form_kb_tab` · `table_view` · `ai_summary` · `side_board` · `follow_tasks` · `timeline` · `emr_progress` · `copy_tools` | หน้าผู้ป่วยและแท็บในแผงขวา (bento ภาพรวม, สรุป CC, งานที่ต้องติดตาม, progress note AI, คัดลอก) |
| `features/patient/` (ลิ้นชัก) | `vs_drawer` · `f9_drawer` | ลิ้นชักวัดสัญญาณชีพซ้ำ (พูด/สั่งวัดเป็นรอบ) · ลิ้นชักส่งต่อผู้ป่วย F9 (ส่งต่อห้อง · สถานะ · เงินรอชำระ · รายการยา) |
| `features/register/` | `register_page` · `face_search` | ลงทะเบียนผู้ป่วยใหม่ (ข้อมูลประจำตัว) · ค้นหาผู้ป่วยด้วยการสแกนหน้า — ปุ่มบนสุดของหน้าแรก |
| `features/order_template/` | `template_view` · `template_aids` · `snake_bite` | Order Set แบบการ์ด + digitize กระดาษ (งูกัด: branch card, lab hint, ความปลอดภัย antivenom, รอบประเมิน, มุมมองกระดาษ) |
| `features/workflow/` | `workflow_state` · `workflow_panel` · `workflow_rail` · `workflow_blocks` · `form_fields` · `step_intro` · `hpi` · `pe_templates` · `icd9` · `disposition` · `esi_assist` | แผงขั้นตอนบันทึก, ฟอร์ม, template, ปลายทาง Admit/Refer |
| `features/speech/` | `speech` · `agent` | ไมค์/ASR · ผู้ช่วย AI (prompt, ตีความ, TTS) |
| `features/alerts/` | `alerts` · `reminders` | แจ้งเตือน bell/toast · เตือนคำสั่งแพทย์ + push notification |
| `features/assistant/` | `chat` | แชตผู้ช่วย + ปุ่มลอย |


---

## 2. บทบาทและ workflow ของโหมดพูด

แต่ละบทบาทกรอก **เฉพาะฟอร์ม HOSxP ที่เป็นของบทบาทนั้น** (`role` ใน `er_form_kb.json`)
กติกา: ห้ามถามข้อมูลที่บทบาทนั้นไม่มีทางรู้ เช่น แพทย์ไม่ถามเรื่องหมวกนิรภัย/แอลกอฮอล์ (เป็นงานพยาบาลคัดกรองที่ถามผู้ป่วย ญาติ หรือผู้นำส่ง)

| บทบาท | ขั้น (orbit) |
|---|---|
| แพทย์ ER | ทบทวนเคส (ยืนยันข้อมูลคัดกรอง/แพ้ยา) → ประวัติ HPI → ตรวจร่างกาย → บาดแผล/หัตถการ → วินิจฉัย/สั่ง → จำหน่าย |
| พยาบาล ER | สัญญาณชีพ → ความรุนแรง AIS → รับคำสั่งแพทย์ → สังเกตอาการ → การพยาบาล → ออกจาก ER |
| พยาบาลคัดกรอง | รับเข้า → อาการสำคัญ → สัญญาณชีพ → GCS/รูม่านตา → อุบัติเหตุ → ระดับ ESI |

ค่าคงที่ในโค้ด: `_doctorSteps/_doctorForm/_doctorAsk`, `_nurse…`, `_triage…` และ `_kbFor(step)` เลือกส่วนของ KB ที่ส่งให้ LLM

### หน้าในแต่ละขั้น (ลำดับการนำเสนอ)

1. การ์ดที่ต้องลงมือทำ (เวลาเป้าหมาย, ชุดคำสั่ง, เลือก template ตรวจร่างกาย ฯลฯ)
2. ฟอร์มครบทุกช่องของขั้น แบบ **flipbook** ทีละช่อง
3. **หน้าสรุปก่อนบันทึก** (ช่อง → ค่า แตะเพื่อแก้) ยืนยันได้เมื่อครบเท่านั้น ครบแล้วระบบพาไปหน้านี้เอง

ตัวควบคุม `‹ n/N ›` นับรวมทุกหน้า (ฟอร์มนับ 1 หน้าต่อช่อง) · สารบัญลอยซ้ายของแผงผู้ช่วย ขอบบนตรงกัน

---

## 3. Generative UI ของผู้ช่วย

- LLM ตอบ JSON: `reply`, `fields`, `choices`, `intent`, `ui` (บล็อกตามแค็ตตาล็อก `erUiCatalogPrompt`)
- บล็อกที่แสดงตัวเลขผู้ป่วย (vitals, labs, imaging) ส่งแค่ **ชื่อ** แอปดึงค่าจริงเอง ผู้ช่วยแต่งตัวเลขไม่ได้
- **ห้ามแสดงซ้ำข้อมูลที่หน้าแสดงอยู่แล้ว**: บล็อก vitals / labs / imaging / brief / alert แพ้ยา ถูกตัดทิ้งตามชนิด (`_spotOf`) ไม่ขึ้นกับแท็บ
- ฟอร์มและหน้าสรุปยืนยันมาจากขั้นเสมอ ผู้ช่วยส่ง form / next_step มาแทนไม่ได้
- ตอบสั้น: prompt จำกัด 10–15 คำ และ `_brief()` ตัดเหลือ 2 ประโยคก่อนแสดง/สร้างเสียง
- สิ่งที่ผู้ใช้ปฏิเสธไปแล้ว (อย่าทำซ้ำ): แผ่นการ์ดลอยกลางจอ, deck ใหญ่, spotlight ชี้แผงเดิม

---

## 4. ช่องกรอกและ master data

- ตัวเลือกมาจาก `er_master_data.json` ก่อน แล้วค่อย KB แล้วค่อย hint
- ช่องโหมดพูดที่รวมหลายช่องจริง จับคู่ตารางด้วย `_fieldTables` (เช่น `GCS (E / V / M)` → gcs_eye/verbal/motor) · `"id|ชื่อ"` = ใช้ตารางซ้ำ (รูม่านตาซ้าย/ขวา) · ค่าเก็บแบบ `a · b` ตามตำแหน่ง
- 2–4 ตัวเลือก = ปุ่มใหญ่เต็มแถว สีตามความหมาย (ปกติเขียว ผิดปกติแดง ไม่ได้ตรวจเทา) · ≤ 8 = ชิป · มากกว่า = รายการค้นหาได้ (`_listSheet`)
- ข้อความยาว: กล่องแก้ไข 4–12 บรรทัด
- คำย่อมีคำอธิบายสั้นใต้ชื่อ (`_terms`) เช่น HEENT → ศีรษะ ตา หู จมูก คอ

### ช่องพิเศษ

| ช่อง | พฤติกรรม |
|---|---|
| HPI | มี template 7 แบบ (`_hpiTemplates`) แนะนำตามประเภทผู้ป่วย · ผู้ช่วยเรียกใช้ด้วย `"hpi_template":"<id>"` แล้วเติม `[ ]` จากคำบอกเล่า ห้ามเดา |
| ตรวจร่างกาย | ทุกระบบมีช่องรายละเอียดอิสระ `"<ระบบ> - รายละเอียด"` · ช่อง "บันทึกการตรวจแบบละเอียด" ท้ายฟอร์ม (= ช่อง "บันทึกการตรวจร่างกาย" ของ HOSxP) · เลือก "ผิดปกติ" แล้วไม่พลิกหน้าเอง |
| Template ตรวจร่างกาย | หน้าแรกของขั้นตรวจร่างกาย = เลือก template · template = **ขอบเขตระบบที่สาขานั้นตรวจ** ระบบนอก template บันทึก "ไม่ได้ตรวจ" และตัดออกจาก flipbook/สารบัญ (`_peScope`, `_stepLabels`) · สร้าง/แก้/ลบในหน้าต่างแยก (`_openPeTemplates`) · เก็บ `shared_preferences` แยกผู้ใช้ · ผู้ช่วยใช้ด้วย `"pe_template":"<ชื่อ>"` |
| ผลตรวจ "ผิดปกติ" | บังคับรายละเอียดเสมอ: เลือกแล้วเปิดกล่องพิมพ์ทันที · ยังไม่ใส่ = ช่องไม่ครบ (`_fieldDone`/`_needsDetail`) ยืนยันบันทึกไม่ได้ |
| ตำแหน่งบาดแผล | แตะบนหุ่น 3D → หา rig bone ใกล้สุด → ชื่อส่วน+ข้าง (เช่น ต้นขาขวา) + หมุดแดง · rig ใช้ `Hip` = ต้นขา, `Palm` = มือ |
| ICD-9-CM | แนะนำจาก master `er_procedure.icd9cm` (ไม่ใช่ LLM เดา) · เลือกหัตถการแล้วเติมให้ทันทีถ้าว่าง |

---

## 5. ไมค์ / เสียง

- **กดค้างเพื่อพูด** ปล่อยเพื่อส่ง (กด < 0.4 วิ หรือไม่ได้ยินเสียงพูด = ยกเลิก · นานสุด 60 วิ)
- ไม่เปิดไมค์เองหลังผู้ช่วยพูด (เคยทำให้เสียงรอบห้องกรอกฟอร์ม/เลื่อนขั้นเอง)
- แยกเสียงพูดจากพื้นหลังห้องด้วย `_dbFloor` (ดังกว่าพื้นหลัง 12 dB)
- สถานะใต้ปุ่ม: กดค้างเพื่อพูด / ปล่อยเพื่อส่ง mm:ss (+ มิเตอร์) / กำลังประมวลผล / น้องช่วยกำลังพูด · คลื่นเสียงรอบปุ่มตามความดังจริง
- บริการ (OpenAI-compatible, BMS Cloud): LLM `vllm-gemma` / `vllm-qwen` (เร็ว), ASR `asr2`, TTS `vox-cpm` · ผู้ช่วยใช้เสียงผู้หญิง "น้องช่วย" ลงท้าย ค่ะ/คะ
- ยังค้าง: ความยินยอมส่งข้อมูลผู้ป่วยขึ้นคลาวด์ (PHI)

---

## 6. ฉาก 3D และ body map

- หุ่นและกายวิภาคมาจาก **BodyParts3D** เท่านั้น ห้ามปั้นเอง (ต้องรองรับทุกอวัยวะ) · spleen/thyroid ไม่มีใน BP3D 4.0
- **ไม่ยุ่งกับชั้นเส้นเลือด** (ไม่ map และไม่โหลด)
- map อาการ → อวัยวะ/กระดูก ต้องเป็น logic ห้าม hard code ต่อเคส (`er_body_map.dart`)
  - กระดูกหัก = กระดูกชิ้นนั้นสีแดง · อาการไม่ระบุตำแหน่ง (ปวดท้อง คลื่นไส้) = heatmap โซน
  - แสดงแบบ x-ray เฉพาะจุด ที่เหลือเป็นผิว รองรับหลายจุดต่อเคส
- ห้ามวางชั้นทับเต็มจอ (BackdropFilter/ClipPath/CustomPaint เจาะรู) บน WebView 3D — Impeller (Vulkan) วาดหน้าหายทั้งแผ่นโดยไม่มี error
  - เบลอครึ่งล่างตอนโหมดพูดใช้ `ShaderMask` + `BackdropFilter` แนวตั้งได้ (ทดสอบแล้ว)
- แก้ JS ในฉากหรือ asset ต้อง hot restart (`R`) ไม่ใช่ reload

---

## 7. แท็บของแฟ้มผู้ป่วย

ภาพรวม · คัดกรอง · ตรวจร่างกาย · คำสั่งแพทย์ · สัญญาณชีพ · ยา · แล็บ · ภาพถ่าย · ฟอร์ม HOSxP (index 0–8)

- **ตรวจร่างกาย** = log ตามเวลา: แถว = ระบบ, คอลัมน์ = ครั้งที่ตรวจ (เก่า → ใหม่) + "ครั้งนี้" · ป้ายเปลี่ยนแปลง พบใหม่/ค่าเปลี่ยน/ดีขึ้น · สลับ ROS/PE
- ส่วนโหมดพูดต้องเหมือนเดิมทุกแท็บ

---

## 8. การทดสอบบนเครื่อง

- แท็บเล็ต Xiaomi (adb `5ced940b`) · แตะด้วยพิกัดจอ 1000×625: `x*2560/1000`, `y*1600/625`
- `flutter run` ผ่าน fifo: `echo r` reload · `echo R` restart
- ห้ามเปิดภาพ screenshot บน Mac เอง (`open`) เว้นแต่ผู้ใช้ขอ
- ห้ามกรอก/ขอ Provider ID หรือ PIN จริง

---

## 9. งานค้าง

- เมนู FAB ของพยาบาลคัดกรองยังเหมือนพยาบาล ER (มี "รับคำสั่งแพทย์")
- ประวัติการตรวจ (log) เป็นชุดจำลองเดียวทุกเคส · คอลัมน์ "ครั้งนี้" ของ ROS ยังไม่ผูกกับโหมดพูด
- เครดิต license BodyParts3D
- build web simulator (`flutter build web --release`) หลังเปลี่ยนแปลง
